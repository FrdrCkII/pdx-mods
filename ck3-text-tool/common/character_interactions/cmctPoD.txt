cmctPoD = {
	category = interaction_category_vassal
	icon = icon_vassal
	desc = cmctCkDesc
	common_interaction = yes

	is_shown = {
		scope:actor = { is_ai = no }
	}

	send_options_exclusive = no
	auto_accept = yes

	send_option = {
		flag = cmctPoD_gainAll_perks_vampire
		localization = "cmctPoD_gainAll_perks_vampire"
	}

	on_accept = {
		scope:actor = {hidden_effect = {
			if = {
				limit = { scope:cmctPoD_gainAll_perks_vampire = yes }
				cmctPoD_gainAll_perks_vampire = yes
			}
		}}
	}
}
